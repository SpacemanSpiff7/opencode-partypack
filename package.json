{
  "name": "opencode-partypack",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "prepare": "git config core.hooksPath hooks"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  },
  "description": "Universal opencode setup — drop-in plugins, agents, commands, and self-improvement gates for any new project."
}
